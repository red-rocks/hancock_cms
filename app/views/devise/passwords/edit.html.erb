<h2><%= t('.change_your_password') %></h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <%= f.hidden_field :reset_password_token %>

  <div class="field mui-textfield mui-textfield--float-label">
    <% if @minimum_password_length %>
      <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em><br />
    <% end %>
    <%= f.password_field :password, autofocus: true, autocomplete: "new-password" %>
    <%= f.label :password, t('.new_password') %>
  </div>

  <div class="field mui-textfield mui-textfield--float-label">
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
    <%= f.label :password_confirmation, t('.confirm_new_password') %>
  </div>

  <div class="actions">
    <%= f.submit t('.change_my_password'), class: "mui-btn mui-btn--raised mui-btn--primary"  %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
